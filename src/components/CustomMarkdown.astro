---
import { twJoin } from 'tailwind-merge';

interface Props {
  class?: string;
  list?: boolean;
}

const { class: className, list = true } = Astro.props;
---

<div
  class={twJoin('markdown', list && '[&>ul]:ml-14 [&>ul]:list-disc', className)}
>
  <slot />
</div>
